# NOTE: In production use SealedSecrets or external secret managers.
# Plain secrets in git are visible to anyone with repo access.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: dev
type: Opaque
stringData:
  POSTGRES_USER: admin
  POSTGRES_PASSWORD: changeme-use-sealed-secrets
  POSTGRES_DB: appdb
