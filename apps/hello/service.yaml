apiVersion: v1
kind: Service
metadata:
  name: hello
  namespace: dev
  labels:
    app: hello
spec:
  type: NodePort
  selector:
    app: hello
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30082
